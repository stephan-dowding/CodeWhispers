h2 Round 3

p Now there is some proper adventuring to do!! A treasure has been dropped on the plain… your task is to determine if you got it! (when you travel over it, you automatically pick it up)

h3 IMPORTANT: Before staring, don't forget to run #[code #{"./reconnect.sh"}] to pull the latest code from server

p The question server will provide:
  pre
    code.
        {
          startX: ~integer~
          startY: ~integer~
          instructions: ~string~
          treasureX: ~integer~
          treasureY: ~integer~
        }

p The answer will expect:
  pre
    code.
      {
        endX: ~integer~
        endY: ~integer~
        treasureFound: ~boolean~
      }

h3 IMPORTANT: When you have completed this round, don't forget to run #[code #{"git push"}]