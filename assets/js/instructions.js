// Generated by CoffeeScript 1.10.0
$(function() {
  var roundSocket, updateInstructions;
  updateInstructions = function(roundNumber) {
    $('.roundNumber').text(roundNumber);
    return $.get("/question/" + roundNumber, function(data) {
      var instructions;
      instructions = $('.instructions');
      instructions.children().remove();
      return instructions.append(data);
    });
  };
  roundSocket = io.connect('/round');
  return roundSocket.on('new round', function(roundNumber) {
    return updateInstructions(roundNumber);
  });
});
