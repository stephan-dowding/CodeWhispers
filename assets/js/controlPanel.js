// Generated by CoffeeScript 1.10.0
$(function() {
  return $('#nextRound').click(function() {
    return $.post('/action/next-round').done(function(data) {
      var header, i, len, movement, ref, row, table;
      $('#round').text("Round " + data.round);
      $('#swapping').children().remove();
      table = $('<table>');
      header = $('<tr>');
      header.append('<th>Origin</th>');
      header.append('<th>Destination</th>');
      table.append(header);
      ref = data.mapping;
      for (i = 0, len = ref.length; i < len; i++) {
        movement = ref[i];
        row = $('<tr>');
        row.append("<th>" + movement.origin + "</th>");
        row.append("<th>" + movement.destination + "</th>");
        table.append(row);
      }
      return $('#swapping').append(table);
    });
  });
});
